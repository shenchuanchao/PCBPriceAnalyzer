<UserControl x:Class="PCBPriceAnalyzer.WPF.Views.PredictionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:PCBPriceAnalyzer.WPF.ViewModels"
             xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:PredictionViewModel}"
        d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 预测条件栏 -->
        <Border Style="{StaticResource CardStyle}" Grid.Row="0">
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Left">
                <TextBlock Text="材料：" VerticalAlignment="Center" Margin="5,0"/>
                <ComboBox ItemsSource="{Binding Materials}" 
                          SelectedItem="{Binding SelectedMaterial}" 
                          DisplayMemberPath="Name" 
                          Width="150" Margin="5,0"/>

                <TextBlock Text="预测日期：" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <DatePicker SelectedDate="{Binding PredictionDate}" Width="120" Margin="5,0"/>

                <Button Content="预测" Command="{Binding PredictCommand}" 
                        Margin="20,0,0,0" Padding="10,5" IsDefault="True"/>
            </StackPanel>
        </Border>

        <!-- 预测结果卡片 -->
        <Border Style="{StaticResource CardStyle}" Grid.Row="1" Margin="0,10,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="预测结果" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding SelectedMaterial.Name}" FontSize="16" Margin="0,0,5,0"/>
                    <TextBlock Text="预测价格：" FontSize="16" Margin="20,0,5,0"/>
                    <TextBlock Text="{Binding PredictedPrice, StringFormat={}{0:N2}}" FontSize="20" FontWeight="Bold" Foreground="Blue"/>
                    <TextBlock Text="{Binding SelectedMaterial.Unit}" FontSize="14" Margin="5,5,0,0"/>
                    <TextBlock Text=" (预测日期: " FontSize="14" Margin="20,0,0,0"/>
                    <TextBlock Text="{Binding PredictionDate, StringFormat={}{0:yyyy-MM-dd}}" FontSize="14"/>
                    <TextBlock Text=")" FontSize="14"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 历史与预测趋势图 -->
        <Border Style="{StaticResource CardStyle}" Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="历史价格与预测趋势" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>

                <ScottPlot:WpfPlot x:Name="PricePlot" Grid.Row="1"/>

                <!-- 图例说明 -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0">
                    <Rectangle Fill="Blue" Width="12" Height="12" Margin="0,0,5,0"/>
                    <TextBlock Text="历史价格" Margin="0,0,15,0"/>
                    <Rectangle Fill="Red" Width="12" Height="12" Margin="0,0,5,0"/>
                    <TextBlock Text="预测价格" Margin="0,0,15,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 加载指示器 -->
        <ProgressBar IsIndeterminate="True" Height="4" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Grid.RowSpan="3" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
