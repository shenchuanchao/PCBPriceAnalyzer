<UserControl x:Class="PCBPriceAnalyzer.WPF.Views.PriceHistoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:PCBPriceAnalyzer.WPF.ViewModels"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:PriceHistoryViewModel}"
        d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 筛选条件栏 -->
        <Border Style="{StaticResource CardStyle}" Grid.Row="0">
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Left">
                <TextBlock Text="材料：" VerticalAlignment="Center" Margin="5,0"/>
                <ComboBox ItemsSource="{Binding Materials}" 
                          SelectedItem="{Binding SelectedMaterial}" 
                          DisplayMemberPath="Name" 
                          Width="150" Margin="5,0"/>

                <TextBlock Text="起始日期：" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <DatePicker SelectedDate="{Binding StartDate}" Width="120" Margin="5,0"/>

                <TextBlock Text="结束日期：" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <DatePicker SelectedDate="{Binding EndDate}" Width="120" Margin="5,0"/>

                <Button Content="查询" Command="{Binding LoadHistoryCommand}" 
                        Margin="20,0,0,0" Padding="10,5" IsDefault="True"/>
            </StackPanel>
        </Border>

        <!-- 价格走势图 -->
        <Border Style="{StaticResource CardStyle}" Grid.Row="1" Margin="0,10,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="价格走势" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
                <ScottPlot:WpfPlot x:Name="PricePlot" Grid.Row="1" Height="250"/>
            </Grid>
        </Border>

        <!-- 历史价格表格 -->
        <Border Style="{StaticResource CardStyle}" Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="历史价格记录" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding PriceRecords}" 
                          AutoGenerateColumns="False" IsReadOnly="True"
                          AlternatingRowBackground="#F5F5F5" 
                          AlternationCount="2">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="日期" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" Width="150"/>
                        <DataGridTextColumn Header="价格" Binding="{Binding Price, StringFormat={}{0:N2}}" Width="120"/>
                        <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="80"/>
                        <DataGridTextColumn Header="来源" Binding="{Binding Source}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- 加载指示器 -->
        <ProgressBar IsIndeterminate="True" Height="4" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Grid.RowSpan="3" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
