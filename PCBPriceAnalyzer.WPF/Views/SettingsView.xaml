<UserControl x:Class="PCBPriceAnalyzer.WPF.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:PCBPriceAnalyzer.WPF.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:SettingsViewModel}"
        d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="应用设置" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <Border Style="{StaticResource CardStyle}" Grid.Row="1">
            <StackPanel Margin="10">
                <TextBlock Text="主题" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox SelectedItem="{Binding Theme}" Margin="0,0,0,15">
                    <ComboBoxItem Content="Light" IsSelected="True"/>
                    <ComboBoxItem Content="Dark"/>
                </ComboBox>

                <TextBlock Text="自动刷新间隔（分钟）" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding AutoRefreshInterval}" Margin="0,0,0,15"/>

                <TextBlock Text="默认材料ID" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding DefaultMaterialId}" Margin="0,0,0,15"/>

                <CheckBox Content="启用价格预警" IsChecked="{Binding EnablePriceAlert}" Margin="0,0,0,15"/>

                <TextBlock Text="价格预警阈值" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding PriceAlertThreshold}" IsEnabled="{Binding EnablePriceAlert}" Margin="0,0,0,15"/>

                <Button Content="保存设置" Command="{Binding SaveSettingsCommand}" 
                        Padding="10,5" HorizontalAlignment="Left" Margin="0,10"/>
            </StackPanel>
        </Border>

        <ProgressBar IsIndeterminate="True" Height="4" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Grid.RowSpan="2" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
